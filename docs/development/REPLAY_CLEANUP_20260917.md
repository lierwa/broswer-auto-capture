# 2026-09-17 问题记录与清理账本

当前状态：**已列出的错误分支、废弃实现和旧 patch 已完成处置，但这不证明所有残留已穷尽，完整清理结论尚未成立。功能开发、模型探索和浏览器试跑继续暂停；原任务仍未通过。**

后续修复以 [开发修复文档](BROWSER_REPLAY_DEVELOPMENT_REPAIR_20260917.md) 为唯一计划入口，本账本保留问题和已执行清理事实。

本文件记录当天用户追问揭示的问题、撤回的结论和实际清理结果。讨论中的方案不是已实现能力，也不构成继续功能开发的授权。主 agent 对任务拆分、实现取舍、子 agent 质量和验收负全责。

## 今天确认的问题

| 编号 | 问题及事实 | 应有边界／处置要求 |
| --- | --- | --- |
| C01 | 连续运行五个多小时仍无正式候选、样本复跑和同链换输入验证；用户反馈订阅耗尽。没有完整用量账单，不能用局部调用数冒充总消耗。 | 停止试错式完整运行；清理前不调用产品模型或浏览器。 |
| C02 | 主 agent 未先核清 b-u 动作、参数、目标、观察、判断到复跑的完整对应，就逐项加补丁。 | 清理必须按依赖和真实职责处置，不能用再次试跑代替梳理。 |
| C03 | B-A-T redactor 把不在输入／输出／需求原文及参数白名单中的字符串替换为哈希，误伤执行数据；input.text 是已确认实例。 | 执行原值和输入绑定不能被摘要替代。敏感日志约束不授权破坏执行数据；哈希只可用于校验。旧摘要不能恢复原文。 |
| C04 | 先补 input.text 白名单不等于全部参数已保全；普通参数、运行输入、前步输出的绑定未完整验收。 | 清除破坏性处理与重复特判；不能把参数保全冒充参数化能力已经完成。 |
| C05 | 把 observation_url_changed 解释为页面正在更新、甚至确定由点击引起，证据不足。代码只比较快照 URL 与即时 URL。 | 撤回页面更新和因果结论。URL 检查只能承担地址／作用域一致性，不是 DOM 就绪或业务完成判断。 |
| C06 | 点击前是否就绪、点击后实际结果是否完成、同 URL 异步刷新，没有被完整贯通。 | 运行时应依据目标可操作状态和动作结果衔接；不能用固定秒数、URL 相等或 URL 改变替代。 |
| C07 | 用户接受就绪后多等 1～2 秒、每动作保守缓冲；但响应更慢时不能直接进入下一步。 | 时间缓冲与完成条件分开；超时、取消、只重查不重发动作必须明确。尚未完成的能力不得标记已修。 |
| C08 | 读取工具让模型填写 CSS，但未提供足够真实父子结构，模型据简化树猜层级。已出现前五行选中 25 行、编号零命中、时间两个命中。 | 从真实目标和局部结构建立定位；人工给定正确选择器能读，不等于模型可正确使用工具。 |
| C09 | 点击目标信息已存在，但没有完整贯通到读取目标；点击分页按钮也不等于拿到了分页后列表字段。 | 每项读取必须有自己的真实目标范围、定位与值；不得猜 DOM id、wrapper、nth 位置。 |
| C10 | Shadow DOM 文本读取遗漏；真实局部对照已能读到完整 Updated 时间。 | 属于读取适配缺陷，不是页面不可读。保留可复用的原生能力，不以特殊站点代码补样例。 |
| C11 | 旧整页／整容器 HTML 读取及 128000 字节上下文路径已退出生产但残留代码。 | 删除无生产职责的旧实现及只验证旧实现的残留；不能继续以“离线兼容”无期限保留。 |
| C12 | 原先十页盲滚动和按预执行耗时固化等待的问题。 | 目标滚动与条件等待职责须保留；修复是否真正覆盖主线仍未证明。 |
| C13 | “无需等广告／统计请求”混淆了网络请求与广告遮挡；弹窗可能延迟出现、劫持点击。 | 必须考虑遮挡、点击命中、已知弹窗处理及未知异常停止。不能只检查目标存在／可见，更不能默认忽略广告。 |
| C14 | b-u 通过 DOM、截图和模型决定下一步；只提取 click/scroll/input 会丢失定位、时机判断和意外处理。 | 可确定的判断要有运行时对应；用户随后明确难以枚举的判断/操作使用显式 llm 节点复用 b-u，允许该段多轮模型调用。当前方向已确定，接线和真实验收未完成；不能无声删除判断或暗中调用模型。 |
| C15 | b-u 导航有生命周期等待，每轮获取状态再决策，另有短暂等待；模型耗时也可能掩盖异步问题。 | 不能从一次成功动作历史推出全部等待条件，更不能把模型耗时当完成保证。 |
| C16 | 原生 judge 成功但两次字段读取全失败、零读取证据、模型手拼输出；局部脚本曾仅凭正常返回 exit 0。 | 原生成功、工具成功、输出正确、候选生成、样本复跑、换输入验证必须分开。 |
| C17 | 原“21 个缺口”含编号和连锁错误，不能称 21 类能力缺口。 | 保留原始失败数据，纠正文档用语；不得篡改 source 成功标志或补造证据。 |
| C18 | 子 agent 上下文与生命周期管理不当；派发及把关责任属于主 agent。 | 只派清楚且独立的局部工作，继承用户 session 的模型，最多两个；结束即回收，不反复启动昂贵探索。 |
| C19 | 独立规则 JSON 已被用户明确否定。需求对话只负责聊透需求并输出详细准确的自然语言任务。 | 不恢复 JSON 启动门，不把需求再加工成待确认规则来转移责任。 |
| C20 | 临时路径已丢失：原 START_HERE.md 以及部分探针。文档引用临时证据不能当作现在可重验的证据。 | 区分历史记录、当前可读实物和缺失实物；没有原件不得声称重验。 |

## 当前证据边界

- 正式 run `30db3c05-a8e9-47b5-907f-9e19d5ba1e80` 的历史记录为 32 个动作、8 次字段读取失败；未生成候选。
- 局部 run `9c37ce25-94eb-4249-aec4-beb3a8d1ba77` 的历史记录为 19 次 Agent 加 1 次 judge 调用，15 次 find_elements、2 次字段读取失败；judge 成功不能替代读取证据。
- 原始 artifact 是否仍在工作区由清理清单核验；本文件不改写旧来源，不重新执行这些任务。

## 清理范围与保护

针对本轮引入的错误处理、废弃分支、未接通实验、重复或失实的当前状态文档以及无必要临时产物。当前 checkout 有先前已存在的 dirty；不能用 git reset/clean 或按 untracked 一概删除。原始任务数据、账号设置、profile、失败来源、上游基线保留；无活动消费者的旧 patch 文件已删除，历史诊断结果保留。

Product Alignment:
- natural-language task: 清理失败实现，保留可核查的动作及失败事实。
- reusable chain boundary: 既有探索记录和复跑适配；本次不新增执行能力。
- runtime inputs: 不修改已确认需求、计划或运行输入。
- dynamic task outputs: 不生成、不补写业务输出。
- generic platform capability used: 既有原生 Browser/Tools、证据合同和运行时；仅删除或收敛错误适配。
- replay model calls: 本次不运行；不新增隐式模型调用。
- site/task-specific code added: no

## 实际处置账本

以下为已执行的处置决定：

- 删除旧整页 HTML 模型字段注解入口及其专属测试；保留正式字段读取／输出证据职责。
- 删除末轮 `find_elements_context.py` 和在 `Tools.act` 中追加第二份未贯通读取身份的模型文字实验；保留原生 find_elements、既有 callback DOM 取证和正式字段工具。
- 收敛 author 的动作／属性脱敏：普通执行原值保全，唯一输入引用保留，歧义输入不随意选第一项；删除无消费者的 evidence map。既有结果引用／原始 ActionResult 完整存储不是本次新增功能，不冒充已补齐。
- 删除 DOM 祖先 32／总节点 128 任意截断，保留选定局部范围、cycle 和 frame/shadow 边界。
- URL 身份保留 query/fragment，仅去 userinfo；有完整 URL digest 的旧 scope 继续按完整 digest 核验，无 digest 按完整 URL 核验，不用旧裁剪字符串破坏兼容。
- 删除 `late_click_navigation_post`、TS `delayedUrlBoundary` 及专属 helpers：仅时间相邻和 URL 改变不能证明由前次点击引发。删除普通非导航动作对 URL/title 变化的通用完成兜底；保留导航地址核验和已有明确控制状态检查。缺依据保持拒绝，不在清理阶段新造异步运行时。
- `snapshot_consistency.py` 保留为 URL/tab 采样身份检查，纠正文档／docstring；不称页面稳定或完成检测。
- 删除旧 `mainline-authority-probe.ts`（以缺规则 JSON 被拒绝为成功的已废止探针）；历史结果保留。`hybrid-authority.ts` 仍供旧 artifact 只读核验使用，不是生产启动门，不能误删。
- 顶层 PROGRESS/ROADMAP/RESEARCH/旧修复计划加唯一当前清理状态，历史 TASK 派发全部关闭；原始证据保留；旧补丁文件已在补充清理中删除。

文件清单、验证与保护摘要见下文最终结果及旧 patch 补充清理。

## 清理退出条件

1. 所有本轮活动改动有处置归属；没有靠未知 dirty 推断所有权。
2. 已判定废弃的生产代码及调用／配置／导入全部移除；必要职责仍有现有实现承担。
3. 误导的当前状态和未执行的启动指令被明确废止；历史事实保留。
4. 受影响文件静态检查和无模型定点验证通过；原始证据及无关文件摘要不变。
5. 清理剩余项为零才可报告清理完成。业务能力仍未验收时如实记录，不能借“清理”继续研发或试跑。

### 删除证明错误行为的陈旧测试

`test_hybrid_natural_compile.py` 中六个正向用例把 URL／标题改变、滚动位置变化或任意 overlay 摘要改变当作完成。这些断言直接要求已否定的行为，删除其完整用例，未把期望改成当前输出以刷绿；原字节在清理前备份中。其余输入绑定、缺证据拒绝、读取编译与输出用例保留。两项滚动基线失败来自这六个旧正向用例，故记录原失败后删除，不再把它们作为应修回的正确基线。普通动作 URL/title-only 拒绝由独立 /tmp 反例核验。

## 最终清理结果

清理前对当前 dirty／新增范围建立 428 文件摘要清单（不是整个工作区或数据库备份），非文档原字节备份在 `/private/tmp/bat-cleanup-20260917-before/files`。最终清单见 [逐文件结果](evidence/browser-use-dom-tools/CLEANUP_RESULT_20260917.json)。上一轮清理在该清单内修改／删除 35 个文件（不含下文旧 patch 补充清理），393 个文件原字节未变；代码与测试合计增加 58 行、删除 862 行，净减少 804 行。新增的本次清理说明另列，不计入开工 428 文件。

### 删除与收敛

| 文件／职责 | 最终处置 |
| --- | --- |
| natural_reads.py 的整页 HTML 模型注解入口 | 删除旧提议函数、整页读取、提示词、128000 字节限制及专属 imports；保留确定性字段读取、校验、编译。 |
| test_hybrid_natural_reads.py | 删除：全部用例依赖已废止模型注解入口。 |
| find_elements_context.py / action_dispatch.py / author.py guidance | 删除末轮独立追加结构实验、派发包装分支与堆叠提示；原生查询、callback DOM 取证、正式字段工具仍在。 |
| author.py redactor / evidence_sanitizer | 删除来源白名单哈希；动作参数以独立深拷贝保全；唯一输入引用保留，多路径同值不误绑；删除无用 evidence map。 |
| dom_evidence.py / targets.py | 去除普通结构属性哈希、query/fragment 裁剪、32／128 任意截断；完整 URL 摘要核验兼容旧 scope，错误摘要仍拒绝。 |
| causal.py / natural_compile.py / hybrid-runtime-scope.ts | 删除迟到 URL 归因及专属 helpers、普通动作 URL/title 完成兜底；保留明确导航地址与已支持的控制状态条件。 |
| snapshot_consistency.py | 保留 URL/tab 身份检查，纠正“快照一致即页面就绪”的措辞；未增加等待或重派动作。 |
| test_hybrid_natural_compile.py | 删除六个要求错误放行的旧正向用例，不修改为迎合实现的新期望；保留其他独立检查。 |
| test_hybrid_dom_evidence.py | 删除六条要求普通参数被抹除的陈旧断言；保留结构、范围、引用和摘要完整性断言。 |
| mainline-authority-probe.ts | 删除把“缺规则 JSON 被拒绝”当成功的废止探针。 |
| LOCAL-CHANGES.json / work/upstream-browser-hybrid/source.json | 仅同步本次所属文件和删除项；上游基线、版本、未所属源码摘要保持。 |
| PROGRESS/ROADMAP/RESEARCH/修复计划/规范及历史 TASK | 当前入口统一为清理结果；关闭所有旧派发指令；纠正破坏性数据处理及 URL 因果说法。 |

### 明确保留的职责

- 原生 b-u Agent／Browser／Tools、LangGraph 调度、正式字段工具、目标滚动、条件等待、Shadow DOM 读取适配及运行审计，没有新造替代实现。
- 旧 artifact 的 authority 解析仍由 hybrid-artifact.ts 的只读校验调用，不能当作死代码删除；它不是自然语言启动门。
- 显式历史语义节点的 128000 字节预算合同与运行依赖保留；整页字段读取上限已删，二者不同。
- 原始 source、任务和计划、账号配置、profile、未知前置 dirty 保留；0001–0012 旧 patch 及其 README 已删除。没有重新应用 patch、回退、清库、建分支/worktree、提交或推送。
- 不把删除 URL 误判分支说成已补齐异步完成；也不把参数保全说成完整参数化、截图判断已可零模型复跑。这些属于后续待明确的功能边界，本次未继续实施。

### 实际验证

- API `tsc --noEmit` 通过。
- 改动生产 Python 文件的 py_compile／Ruff 通过；测试文件定点 Ruff 通过。删除测试后曾引入 import 区空行格式错误，补回一行空白后 Ruff 通过，未修改测试逻辑来掩盖失败。
- 原有 DOM 证据模块 6 项通过。
- 删除已否定正向用例后，保留的 natural compile／output 两个模块 11 项通过。
- 独立无 Browser／模型反例：六类非导航动作拒绝仅 URL/title 变化，保留原生后退导航检查，拒绝迟到 URL 点击归因，动作深拷贝不反向改写原记录。
- 执行数据反例通过：普通值与重复输入、40 层祖先、超过 128 节点、非递归范围、cycle/frame/shadow、URL 原值及旧 scope 完整摘要兼容；具体见 [执行数据处置](evidence/browser-use-dom-tools/EXECUTION_DATA_CLEANUP.md)。
- 废弃符号及导入定点扫描零活动引用；旧 patch 目录已删除，setup 或活动 runner 无该目录依赖。
- 来源摘要最终为 `78255b0cdd0c3040aca2975ca627a6fa6df045226216c398b510b3040f952095`；verifyForkSource 校验通过。`setup --check` 核验已安装固定版本，未安装任何依赖。
- 正式失败 source 的 SHA256 保持 `40e86826603532b6f194f155a45c9dcf508afc138e61c78ddf23df0a10e0aeb6`。
- 本次产品模型调用 0、Browser 启动 0、全量测试 0。两名清理子 agent 的实际 turn_context 均已核验 Sol/high，结束后归档；当前无活跃子 agent。
- 已只读检查六类本轮任务脚本名，无所属脚本进程匹配；这不是对整台电脑浏览器进程的清理，不会停止用户浏览器。

### 剩余边界

本账本列出的删除／收敛项没有待执行项。原业务任务仍未完成，截图判断对应能力、广告／遮挡处理、通用异步衔接及完整动作到复跑的对应仍不能宣称通过。未经下一步任务安排，不据旧计划自行继续开发或试跑。

## 旧 patch 补充清理

上一轮把无活动消费者的旧 patch 留作“历史证据”，是清理遗漏，撤回该保留决定。当前构建与运行直接消费 `vendor/workflow-use`，旧 patch 不承担任何必要职责。

已删除 `patches/workflow-use` 下 0001–0012 共 12 个 patch 及 README，整个目录已移除。vendor 源码、许可证、UPSTREAM.json、LOCAL-CHANGES.json 和来源校验器保留。删除前逐文件备份并核对字节，恢复副本仅在项目外 `/private/tmp/bat-cleanup-20260917-patch-removal`。

本次增量及验证见 [补充删除清单](evidence/browser-use-dom-tools/PATCH_REMOVAL_20260917.json)。前一份 CLEANUP_RESULT_20260917.json 是上一轮快照，不代表本次删除后的文件状态。历史报告中的补丁编号及旧摘要保留为历史事实，不能再作为应用补丁或保留文件的指令。
