# F6 完整批量验收授权方案

当前海尔验收需求保持不变：海尔中国官网冰箱目录，名称、型号、链接及字段缺失说明；不采集价格、评论或购买信息。正式来源与计划绑定来自隔离验收任务，用户 data 不参与。

## 预算依据

F6 正式 Worker 已输出 258 个去重目录链接，目录消耗 157 条命令、约 33 秒、0 次模型调用。此数量来自本次图输出，商品锚点终止仍需核验当前实际末页，不能单独形成完整覆盖声明。

现有详情步骤上限 140 条命令；一次 navigate + page 至少约 7 条底层命令，258 个详情仅打开及读取就需要约 1806 条，另有探索、验证和恢复核验。旧计划的步骤预算和 500 条全局上限不能支撑本次完整范围。

## 已授权的新计划上限

| 步骤 | 浏览器命令 | 时间 | 探索或修复判断 | 显式 LLM |
| --- | ---: | ---: | ---: | ---: |
| 目录枚举及当前末页核验 | 300 | 180 秒 | 8 | 0 |
| 全部详情字段 | 3500 | 1200 秒 | 3 | 0 |
| 已确认缺失规则派生 | 50 | 60 秒 | 1 | 0 |
| 合计 | 3850 | 1440 秒（24 分钟） | 12 | 0 |

2026-09-06 用户明确回复“授权此新预算与完整验收”。已调整程序支持上限，并通过正式计划 API 生成 v6（8bca241e-0760-44b9-b6df-55ff93031e83），逐步骤预算与上表一致。默认计划仍受原 500 条/300 秒上限约束，历史计划与运行保留原预算。预算耗尽、访问限制和结构漂移仍暂停，保留真实剩余范围。

首次执行 0a5f476a-b8c6-4b84-abc1-08b03eeae132 在探索阶段暂停：点击分页标签和下一页、按 Enter 均未改变目录。消耗 57 条命令、128811ms、6 次 Sol/high 探索判断、0 次显式 LLM；未进入批量输出。具体交互判断和浏览器回收事实保留在忽略目录的 f6-full 同名运行证据中。完整覆盖待分页交互恢复后继续验收。

完整目录的起点、当前末页、跨页去重及字段覆盖必须另有实测证据。旗舰店与第二站点分别按其正式来源和计划验收；本方案只明确已具备真实来源证据的海尔目录预算。
