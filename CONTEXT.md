# 浏览器抓取工具术语

本工具将自然语言抓取需求转化为可验证、可保存并可换输入复用的抓取链路。

## Language

**抓取需求**：
通过对话明确的抓取目标、来源范围、内容范围和完成标准。

**抓取计划**：
为满足抓取需求而安排的抓取步骤，以及步骤之间的依赖、输入和预期产出。

**抓取步骤**：
抓取计划中具有独立目标和完成标准的工作单元，由一条抓取链路实现。

**动作节点**：
抓取链路中的执行单元，包括由程序完成的普通动作节点和明确使用模型能力的 LLM 节点。

**普通动作节点**：
按照已确定规则完成浏览器操作或数据处理的动作节点，其执行无需模型调用。

**LLM 节点**：
抓取链路中明确要求模型理解、判断或生成能力的动作节点。

**抓取链路**：
实现抓取步骤的可执行动作节点及其连接关系，包含复用所需的输入、执行条件、结果检查和完成条件。

**探索运行**：
由模型理解目标并借助 BrowserSkill 探索实际操作方法、取得结果证据的运行，为形成抓取链路提供依据。

**链路验证**：
核对抓取链路是否满足步骤完成标准及其声明的复用范围的过程。

**复跑运行**：
向已保存的抓取链路提供输入，由程序执行普通节点，并仅在到达明确的 LLM 节点时调用模型的运行。

**零模型调用复跑**：
从启动到结束均未调用模型的复跑运行，以该运行实际发生的模型调用记录核验。
