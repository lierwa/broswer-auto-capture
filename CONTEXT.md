# 浏览器自动化任务链路术语

本术语表定义 B-A-T 将自然语言浏览器任务转化为可验证、可复跑任务链路时使用的统一语言。数据采集只是浏览器自动化任务的一种类别。

## Language

**浏览器自动化任务**：
用户以自然语言提出、需要浏览器参与并以可观察结果判定完成的目标。

**任务链路系统**：
把浏览器自动化任务转化为参数化任务链路，并负责保存、验证、复跑、恢复和审计的产品。
_Avoid_：抓取器、京东采集系统、浏览器录像工具

**任务类别**：
按最终结果划分的任务类型，用于选择领域知识和完成标准；混合任务可以组合多个类别，不要求用户预先选择。

**任务需求**：
通过对话确认的任务目标、范围、约束和完成标准。

**数据采集**：
以具有明确实体、字段和范围的结构化数据集合为最终结果的浏览器自动化任务，并保留来源关联和覆盖说明。

**抓取需求**：
数据采集任务的任务需求，明确采集目标、来源范围、内容范围和完成标准。

**需求访谈**：
围绕同一任务需求持续澄清取舍的对话，按最终结果选择领域知识，包含已确认决策和待决事项。
_Avoid_：一句话生成计划、需求弹窗

**需求决策**：
用户对范围或取舍问题的明确选择，区别于尚未获确认的系统建议。

**访谈轮次**：
围绕一次用户输入完成理解、提问或修订草稿的一次处理；失败后的重试属于新的处理尝试，仍关联原始输入。

**待决事项**：
访谈中尚需负责人明确取舍的问题；用户补充或追问不自动等于对建议的采纳。

**草稿确认**：
用户对某个当前有效需求版本的明确认可；后续纠正使旧确认不再适用于当前需求，但历史确认仍保留。

**需求草稿**：
从访谈中沉淀、供用户审阅的版本化需求说明，描述目标、范围、约束和完成标准。
_Avoid_：任务计划、任务链路

**来源取证**：
Planning Run 在当前任务确有需要时寻找并实际检查入口、代表页面或外部来源的工作，不是所有浏览器任务的固定前置阶段。

**来源证据**：
对具体来源及其内容、访问条件和覆盖范围的可追溯观察，具有观察时间和适用边界。

**任务计划**：
依据已确认需求及按需取得的浏览器证据形成、用于授权和指导后续自动化执行的版本化安排，包含任务步骤、依赖、输入输出、预算和完成条件。
_Avoid_：需求摘要、节点示意图

**任务步骤**：
任务计划中具有独立目标和完成标准的工作单元，由一条任务链路实现。

**任务链路**：
实现一个任务步骤的参数化可执行动作图，包含输入、节点、连接、结果检查、完成条件及可安全复用的边界。链路不绑定某次运行的临时页面引用或固定业务样本。

**链路组合**：
任务计划通过上下游输入输出连接多条任务链路；上游产生的集合可以逐项调用同一条下游链路。

**链路调用**：
一条任务链路以明确输入调用另一条已保存链路，并接收其输出；逐项处理集合仍是复跑同一条被调用链路。

**任务数据契约**：
某个任务版本声明的动态输入和输出结构，只约束该任务及其链路，不成为平台固定业务类型。

**值绑定**：
节点从任务输入、上游节点输出、运行变量或常量取得参数，并把结果写入后续节点可引用的位置。

**动作节点**：
任务链路中的执行单元，包括浏览器操作、观察、数据处理、条件、循环、链路调用、人工等待、检查点、输出、终止及明确使用模型能力的节点。

**意图目标**：
动作派发前由 browser-use 原生 selector map 确定的目标及其局部结构，只说明动作准备作用于哪里，不证明事件最终命中了该节点。

**事件命中目标**：
浏览器在事件分发当刻通过 `event.target` 实际暴露给所属 document 监听器的节点；与该事件的 `composedPath()`、document/frame 身份和事件来源一同记录。它不得由意图目标或动作结束后的页面状态补写。

**普通动作节点**：
按照已确定规则完成浏览器操作、控制流或数据处理的动作节点，其执行无需模型调用。

**LLM 节点**：
任务链路中明确要求模型理解、判断或生成能力的动作节点。

**探索运行**：
由模型理解任务步骤并借助浏览器能力探索实际操作方法、取得结果证据的运行，为编译参数化任务链路提供依据。

**链路验证**：
用代表输入和不同输入核对任务链路是否满足完成标准、是否真正使用运行参数，以及其声明复用范围是否成立的过程。

**复跑运行**：
向已保存的任务链路提供输入，由程序执行普通节点，并仅在到达明确的 LLM 节点时调用模型的运行。

**零模型调用复跑**：
从启动到结束均未调用模型的复跑运行，以该运行实际发生的模型调用记录核验。

**批量运行**：
在某次计划授权内把输入集合逐项交给同一条任务链路复跑的运行，保存每项进度、结果、覆盖依据与未完成范围。

**同运行恢复**：
核验浏览器与原检查点绑定后，在原运行及其剩余授权预算内继续；已经保存的来源记录和消耗不会清零。

**人工等待点**：
任务计划取证或授权执行遇到登录、验证码、确认及访问限制时保存的可恢复状态。自动化停止在当前页面并通过 BrowserSkill 请求用户处理；等待不消耗自动化活动预算，完成后必须重新观察页面再继续同一 Plan 或 Execution。

**覆盖结论**：
依据本次运行实际枚举、终止依据与字段采集情况形成的完成程度；链路结束和代表样本通过分别是证据，不单独等于全范围完成。

**修复授权**：
用户针对受影响步骤发起的模型修复许可；修复形成并验证新链路版本，原版本及运行历史保留。
